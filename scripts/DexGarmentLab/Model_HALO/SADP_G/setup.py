from setuptools import find_packages, setup

setup(
    name="structure_aware_diffusion_policy_garment",
    version="0.1.0",
    packages=find_packages(),
    description="An structure aware diffusion policy with garment implementation for DexGarmentLab",
    python_requires=">=3.8",
)
